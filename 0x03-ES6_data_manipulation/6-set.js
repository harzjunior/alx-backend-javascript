/**
 * Creates a set from an arr.
 * @param {Array} arr - The source arr.
 * @author Haruna Bah Jibril <https://github.com/harzjunior>
 * @returns {Set}
 */
export default function setFromArray(array) {
  return new Set(array);
}

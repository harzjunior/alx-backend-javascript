/**
 * Shows a msg on the STDOUT.
 * @param {String} msg to show.
 * @author Haruna Bah Jibril <https://github.com/harzjunior>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
